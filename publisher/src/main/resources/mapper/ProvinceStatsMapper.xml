<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="issac.mapper.ProvinceStatsMapper">
    <select id="selectProvinceStats" resultType="issac.bean.ProvinceStats">
        select province_name, sum(order_amount) order_amount
        from province_stats
        where toYYYYMMDD(stt) = #{date}
        group by province_id, province_name
    </select>
</mapper>